APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# ════════════════════════════════════════════════════════
# CONFIGURACIÓN DE EMAIL
# ════════════════════════════════════════════════════════

# Opciones de MAIL_MAILER:
# - log: Solo guarda en logs (desarrollo sin envío real)
# - smtp: Envía emails reales (producción/testing)

MAIL_MAILER=smtp
MAIL_SCHEME=null

# ──────────────────────────────────────────────────────
# OPCIÓN 1: SENDER.NET (RECOMENDADO)
# ──────────────────────────────────────────────────────
# Plan gratuito: 2,500 emails/mes
# Ventajas:
# - Sin restricciones en testing
# - Envía a cualquier email desde el día 1
# - Panel con estadísticas completas
# - Plantillas visuales incluidas
#
# Configuración:
# 1. Crear cuenta en https://www.sender.net/
# 2. Ir a Settings → SMTP Settings
# 3. Activar SMTP y copiar credenciales
#
MAIL_HOST=smtp.sender.net
MAIL_PORT=587
MAIL_USERNAME=tu_email@ejemplo.com
MAIL_PASSWORD=SND_tu_token_aqui
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=tu_email@ejemplo.com
MAIL_FROM_NAME="Estoicos Gym"

# ──────────────────────────────────────────────────────
# OPCIÓN 2: RESEND (alternativa)
# ──────────────────────────────────────────────────────
# Plan gratuito: 100 emails/mes
# NOTA: En modo testing solo envía a email verificado
#
# MAIL_HOST=smtp.resend.com
# MAIL_PORT=587
# MAIL_USERNAME=resend
# MAIL_PASSWORD=re_tu_api_key
# MAIL_ENCRYPTION=tls
# MAIL_FROM_ADDRESS=tu_email@ejemplo.com
# MAIL_FROM_NAME="Estoicos Gym"

# ──────────────────────────────────────────────────────
# OPCIÓN 3: MAILTRAP (solo para desarrollo)
# ──────────────────────────────────────────────────────
# No envía emails reales, solo los captura para testing
# Ideal para desarrollo local sin spam
#
# MAIL_HOST=sandbox.smtp.mailtrap.io
# MAIL_PORT=2525
# MAIL_USERNAME=tu_username_mailtrap
# MAIL_PASSWORD=tu_password_mailtrap
# MAIL_ENCRYPTION=tls
# MAIL_FROM_ADDRESS=noreply@estoicosgym.local
# MAIL_FROM_NAME="Estoicos Gym [DEV]"

# ──────────────────────────────────────────────────────
# OPCIÓN 4: GMAIL (backup, no recomendado para producción)
# ──────────────────────────────────────────────────────
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=tu_correo@gmail.com
# MAIL_PASSWORD=tu_contraseña_de_aplicacion
# MAIL_ENCRYPTION=tls
# MAIL_FROM_ADDRESS=tu_correo@gmail.com
# MAIL_FROM_NAME="Estoicos Gym"
#
# Para Gmail:
# 1. Habilitar 2FA: https://myaccount.google.com/security
# 2. Crear "Contraseña de aplicación": https://myaccount.google.com/apppasswords
# 3. Usar esa contraseña en MAIL_PASSWORD

# ──────────────────────────────────────────────────────
# DESARROLLO LOCAL (sin envío)
# ──────────────────────────────────────────────────────
# Para desarrollo sin enviar emails reales:
# MAIL_MAILER=log
# (Los emails se guardan en storage/logs/laravel.log)

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false
